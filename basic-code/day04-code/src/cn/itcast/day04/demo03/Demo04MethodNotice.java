package cn.itcast.day04.demo03;
/*
使用方法时的注意事项：

1.方法应该定义在类中，不能再方法中再定义方法，不能嵌套。
2.方法定义的前后顺序无所谓。
3.方法定义之后需要调用才会执行：单独调用，打印调用，赋值调用。
4.如果方法有返回值 必须写：return 返回值；不能没有。
5.return后的返回值数据必须和方法的返回值类型对照一致。
6.对于一个void方法 只能写：return ；
7.对于方法中最后一行的return可以不写
8.对于方法中可以有多个return，但是必须保证同时只有一个会被执行到。
 */
public class Demo04MethodNotice {
}
